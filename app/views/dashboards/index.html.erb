<%= "Login as : #{current_user.email}, role : #{current_user.role}" %></br>

<%= link_to "Homepage", homepages_path %>

<% if can? :edit_info, current_user %>
	<%= link_to "Edit Info", edit_info_path(current_user.id) %>
<% end %>



<% if can? :user_approval, current_user %>
	<%= link_to "User Approval", members_path %>
<% end %>

<% if can? :kim_approval, current_user %>
	<%= link_to "KIM Approval (by Admin)", kim_approval_path %>
<% end %>

<% if can? :create_feedback, current_user %>
	<%= link_to "Create Feedback", new_feedback_path %>
<% end %>

<% if can? :view_feedback, current_user %>
	<%= link_to "View Feedback", feedbacks_path %>
<% end %>

<% if can? :create_lkp, current_user %>
	<%= link_to "Create LKP", new_lkp_path %>
<% end %>

<% if can? :view_lkp, current_user %>
	<%= link_to "View LKP", lkps_path %>
<% end %>

<% if can? :create_admin, current_user %>
	<%= link_to "Admin", admins_path %>
<% end %>

<% if can? :create_crew, current_user %>
	<%= link_to "Crew", crews_path %>
<% end %>


<% if can? :create_kim, current_user %>
	<%= link_to "Create KIM", kimms_path %>
<% end %>

<% if can? :kim_approval_by_crew, current_user %>
	<%= link_to "KIM Approval (by Crew)", new_dashboard_path %>
<% end %>



<%= link_to "Change password", destroy_user_session_path(:reset_password=>current_user.id), :method => :delete %>
<%= link_to('Logout', destroy_user_session_path, :method => :delete) %> 


<br>
Profile Picture:
<%= image_tag current_user.photo.url(:medium) %>

<br><br>


<% if params[:rollback_id] %>
	$("#modal").html("<%= escape_javascript(render 'kimms/modal') %>");
	$("#rollback").modal("show");
<% else %>
	$("#kim_approval").html("<%= escape_javascript(render 'kimms/kim_approval') %>");
<% end %>